"""Pure-Python building blocks of the Geodit sync client.

Nothing in this package may import ``qgis`` or ``osgeo``: it is the part of
the plugin that runs (and is unit-tested) on any Python >= 3.9.
"""
